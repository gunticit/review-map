<VirtualHost *:80>
  ServerName doitac.rivi.test.abc
  ServerAlias doitac.rivi.test.abc
  ServerAdmin webmaster@localhost

  ProxyPassMatch ^/(.*\.php(/.*)?)$ fcgi://php:9000${DOCUMENT_ROOT}/

  DocumentRoot ${DOCUMENT_ROOT}

  ErrorLog ${APACHE_LOG_DIR}/error.log
  CustomLog ${APACHE_LOG_DIR}/access.log combined

  <Directory ${DOCUMENT_ROOT}>
    Options Indexes FollowSymLinks
    AllowOverride All
    Require all granted
    DirectoryIndex index.php
  </Directory>
</VirtualHost>

<VirtualHost *:80>
  ServerName quantri.rivi.test.abc
  ServerAlias quantri.rivi.test.abc
  ServerAdmin webmaster@localhost

  ProxyPassMatch ^/(.*\.php(/.*)?)$ fcgi://php:9000${DOCUMENT_ROOT}/

  DocumentRoot ${DOCUMENT_ROOT}

  ErrorLog ${APACHE_LOG_DIR}/error.log
  CustomLog ${APACHE_LOG_DIR}/access.log combined

  <Directory ${DOCUMENT_ROOT}>
    Options Indexes FollowSymLinks
    AllowOverride All
    Require all granted
    DirectoryIndex index.php
  </Directory>
</VirtualHost>

<VirtualHost *:80>
  ServerName khachhang.rivi.test.abc
  ServerAlias khachhang.rivi.test.abc
  ServerAdmin webmaster@localhost

  ProxyPassMatch ^/(.*\.php(/.*)?)$ fcgi://php:9000${DOCUMENT_ROOT}/

  DocumentRoot ${DOCUMENT_ROOT}

  ErrorLog ${APACHE_LOG_DIR}/error.log
  CustomLog ${APACHE_LOG_DIR}/access.log combined

  <Directory ${DOCUMENT_ROOT}>
    Options Indexes FollowSymLinks
    AllowOverride All
    Require all granted
    DirectoryIndex index.php
  </Directory>
</VirtualHost>
